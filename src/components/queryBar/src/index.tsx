import { defineComponent } from 'vue'

export default defineComponent({
  name: 'QueryBar',
  props: {},
  setup() {
    return () => (
      <div class="w-full  flex items-center">

      </div>
    )
  },
})
